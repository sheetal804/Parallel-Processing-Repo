echo enter two strings
read s
read s1
if [ -z $s ]
then
	if [ -z $s1 ]
	then 
	echo not null strings and they exist
	else
	echo second string is null nd does not exist
	fi
else
	if [ -z $s1]
	then
	echo second string exist but first doesnot exist
	else
	echo both strings doesnot exist and are null
	fi
fi
